const { Client } = require('pg'); const client = new Client({ user: 'postgres', host: 'localhost', database: 'smart_parking', password: 'Okay@123', port: 5432 }); client.connect().then(async () => { try { await client.query('INSERT INTO users (email, password, full_name, phone, role) VALUES (\, \, \, \, \) RETURNING *', ['test_new@test.com', 'pwd', 'Test', '123', 'finder']); console.log('SUCCESS'); } catch(e) { console.error('ERROR:', e.message); } finally { client.end(); } });
